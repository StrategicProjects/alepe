% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aliases.R
\name{alepe_aliases}
\alias{alepe_aliases}
\alias{alepe_parlamentares}
\alias{alepe_servidores}
\alias{alepe_cargos}
\alias{alepe_lotacoes}
\alias{alepe_remuneracao}
\alias{alepe_contratos}
\alias{alepe_licitacoes}
\alias{alepe_projetos}
\alias{alepe_indicacoes}
\alias{alepe_requerimentos}
\alias{alepe_limpar_cache}
\title{Portuguese aliases}
\usage{
alepe_parlamentares(refresh = FALSE)

alepe_servidores(status = NULL, refresh = FALSE)

alepe_cargos(status = NULL, refresh = FALSE)

alepe_lotacoes(refresh = FALSE)

alepe_remuneracao(refresh = FALSE)

alepe_contratos(refresh = FALSE)

alepe_licitacoes(refresh = FALSE)

alepe_projetos(numero = NULL, ano = NULL, legislatura = NULL, refresh = FALSE)

alepe_indicacoes(
  numero = NULL,
  ano = NULL,
  legislatura = NULL,
  refresh = FALSE
)

alepe_requerimentos(
  numero = NULL,
  ano = NULL,
  legislatura = NULL,
  refresh = FALSE
)

alepe_limpar_cache()
}
\arguments{
\item{refresh}{If \code{TRUE}, bypass the local cache.}

\item{status}{Employment status filter. One of \code{"permanent"},
\code{"commissioned"}, or \code{"seconded"} (the original API terms
\code{"efetivo"}, \code{"comissionado"}, and \code{"a-disposicao"} are also
accepted), or \code{NULL} (default) for all.}

\item{numero}{Portuguese spelling of \code{number}.}

\item{ano}{Portuguese spelling of \code{year}.}

\item{legislatura}{Portuguese spelling of \code{legislature}.}
}
\value{
The same tibble the corresponding English function returns.
}
\description{
Every endpoint function has an alias named after the API endpoint it
wraps, for analysts who would rather keep their whole pipeline in
Portuguese. The aliases are ordinary functions with the same
arguments, the same defaults and the same return value as their
English counterparts — only the name differs.
}
\details{
\tabular{ll}{
   Portuguese \tab English \cr
   \code{alepe_parlamentares()} \tab \code{\link[=alepe_representatives]{alepe_representatives()}} \cr
   \code{alepe_servidores()} \tab \code{\link[=alepe_staff]{alepe_staff()}} \cr
   \code{alepe_cargos()} \tab \code{\link[=alepe_positions]{alepe_positions()}} \cr
   \code{alepe_lotacoes()} \tab \code{\link[=alepe_departments]{alepe_departments()}} \cr
   \code{alepe_remuneracao()} \tab \code{\link[=alepe_remuneration]{alepe_remuneration()}} \cr
   \code{alepe_contratos()} \tab \code{\link[=alepe_contracts]{alepe_contracts()}} \cr
   \code{alepe_licitacoes()} \tab \code{\link[=alepe_procurements]{alepe_procurements()}} \cr
   \code{alepe_projetos()} \tab \code{\link[=alepe_bills]{alepe_bills()}} \cr
   \code{alepe_indicacoes()} \tab \code{\link[=alepe_indications]{alepe_indications()}} \cr
   \code{alepe_requerimentos()} \tab \code{\link[=alepe_requests]{alepe_requests()}} \cr
   \code{alepe_limpar_cache()} \tab \code{\link[=alepe_cache_clear]{alepe_cache_clear()}} \cr
}


Names are written without accents, since accented characters in R
identifiers are awkward to type and travel badly between locales.

Argument \emph{values} follow the same both-ways rule: \code{status} accepts
the English vocabulary and the original API terms alike, so
\code{alepe_servidores(status = "efetivo")} and
\code{alepe_staff(status = "permanent")} are the same query.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
alepe_parlamentares()
alepe_servidores(status = "efetivo")
alepe_projetos(ano = 2024)
\dontshow{\}) # examplesIf}
}
