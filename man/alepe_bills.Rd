% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propositions.R
\name{alepe_bills}
\alias{alepe_bills}
\alias{alepe_indications}
\alias{alepe_requests}
\title{Query legislative propositions}
\usage{
alepe_bills(number = NULL, year = NULL, legislature = NULL, refresh = FALSE)

alepe_indications(
  number = NULL,
  year = NULL,
  legislature = NULL,
  refresh = FALSE
)

alepe_requests(number = NULL, year = NULL, legislature = NULL, refresh = FALSE)
}
\arguments{
\item{number}{Proposition number, for the detail mode. Requires
\code{year}.}

\item{year}{Filter by year (listing mode) or select the proposition
(detail mode).}

\item{legislature}{Filter by legislature number (listing mode).}

\item{refresh}{If \code{TRUE}, bypass the local cache.}
}
\value{
A tibble. In listing mode, one row per proposition with
\code{docid}, \code{numero}, \code{ano}, \code{legislatura}, \code{tipo}, \code{subtipo},
\code{ementa}, \code{data_publicacao} (\code{Date}), and \code{autores} (author names,
\verb{;}-separated). In detail mode, a single row with \code{numero}, \code{ano},
\code{legislatura}, \code{tipo}, \code{autores}, \code{ementa}, \code{materia},
\code{justificativa}, \code{regime_tramitacao}, \code{impacto_orcamentario},
\code{resultado_final}, \code{data_publicacao} (\code{Date}), \code{numero_dpl}, and
\code{lotacao_atual}. On network failure, a zero-column tibble (with a
warning).
}
\description{
Retrieves legislative propositions from the Assembly: bills
(\code{alepe_bills()}), indications (\code{alepe_indications()}), and requests
(\code{alepe_requests()}). Two query modes are supported by the API:
}
\details{
\itemize{
\item \strong{Detail}: pass \code{number} and \code{year} to fetch the full record of a
single proposition.
\item \strong{Listing}: pass \code{year} and/or \code{legislature} to fetch summaries of
multiple propositions. When no filter is given, the API defaults to
the current year.
}

These endpoints serve structured data as XML embedded in CSV; the
package parses the XML fields into regular columns, with names
normalized to snake_case and HTML markup stripped from free-text
fields.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# All bills of the current year
alepe_bills()

# One specific bill
alepe_bills(number = 100, year = 2024)
\dontshow{\}) # examplesIf}
}
